const message = "Hello World";
const bool = true;
let num = 42;

num = 5;
// bool = false; // gives error

const nonExistent = undefined;
const empty = null;

console.log(message, bool, num, nonExistent, empty);
document.write(message);
